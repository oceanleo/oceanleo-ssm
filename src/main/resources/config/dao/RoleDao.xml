<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ocean.project.ssm.dao.RoleDao">
  <resultMap id="BaseResultMap" type="com.ocean.project.ssm.domain.Role">
    <id column="id" property="id"/>
    <result column="role_name" property="roleName"/>
    <result column="role_code" property="roleCode"/>
    <result column="date_create" property="dateCreate"/>
    <result column="date_update" property="dateUpdate"/>
    <result column="date_delete" property="dateDelete"/>
    <result column="create_id" property="createId"/>
    <result column="update_id" property="updateId"/>
    <result column="delete_id" property="deleteId"/>
    <result column="deleted" property="deleted"/>
    <result column="version" property="version"/>
  </resultMap>

  <insert id="insert" parameterType="Role">
    insert into role(
      id,role_name,role_code,date_create,date_update,date_delete,
      create_id,update_id,delete_id,deleted,version)
    values(
      #{id},#{roleName},#{roleCode},#{dateCreate},#{dateUpdate},#{dateDelete},
      #{createId},#{updateId},#{deleteId},#{deleted},#{version})
  </insert>

  <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
    select r.id,r.role_name,r.role_code from role r
    inner join user_role ur on r.id = ur.role_id
    inner join user u on u.id = ur.user_id
    where r.deleted=0 and ur.deleted=0 and u.deleted=0 and u.user_name = #{username}
  </select>

  <select id="selectAll" resultMap="BaseResultMap">
    select id,role_name,role_code from role where deleted=0
  </select>
</mapper>